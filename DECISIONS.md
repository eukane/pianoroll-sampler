# 설계 판단 기록

오디오 관련 판단은 나중에 보면 이유가 기억나지 않는다. **무엇을 골랐는지가
아니라 왜 골랐는지**를 남긴다.

---

## M1

### 노트는 악기 정보를 갖지 않는다

이 앱의 핵심 기능이 "같은 멜로디를 다른 악기로 바꿔 듣기" 다. 노트에 악기가
붙어 있으면 악기를 바꿀 때 노트를 전부 순회하며 고쳐야 하고, 그 과정에서
데이터가 상한다. 악기는 트랙에 붙이고, 트랙의 `source` 한 줄만 바꾼다.

이 원칙을 코드로 고정한 자리가 `audio/registry.ts` 다. 피아노롤도 스케줄러도
트랙이 무슨 음원을 쓰는지 모르고, 레지스트리에게 `Instrument` 를 달라고만
한다. M2 에서 SF2 샘플러를 붙일 때 고칠 파일이 여기 하나가 되도록 했다.

### 시간 단위는 초가 아니라 박(beat)

BPM 을 바꿨을 때 노트를 다시 계산해야 한다면 그건 데이터 모델이 잘못된 것이다.
모델은 전부 박으로 들고, 초로의 환산은 재생·렌더 시점에만 한다.

### 재생은 lookahead 스케줄러로 (25ms 간격 / 120ms 앞서 예약)

`setInterval` 이 부르는 순간에 소리를 내면 박자가 흔들린다. 타이머는 수십 ms 씩
밀리고, 폰에서는 스크롤만 해도 더 밀린다.

그래서 타이머는 **예약만** 하고 실제 시각은 AudioContext 시계에 맡긴다.
25ms 마다 깨어나 앞으로 120ms 안에 시작할 노트를 절대 시각으로 예약해 둔다.

120ms 는 타협값이다. 짧으면 타이머가 한 번 밀렸을 때 노트를 놓치고, 길면
재생 중에 노트를 고쳤을 때 반영이 늦다.

재생 헤드 위치는 예약 상태와 **따로** 계산한다. 예약은 미래로 앞서 나가 있어서
그걸로 위치를 그리면 헤드가 소리보다 앞질러 간다.

브라우저 자동 점검(`npm run smoke`)에서 실제 재생 속도를 재고 있다.
BPM 120 설정에서 오차 3 BPM 이내를 통과 조건으로 뒀다.

### 폴리포니 상한 24, 초과하면 가장 오래된 소리를 뺏는다

소리 취향이 아니라 성능 문제다. 폰에서 오실레이터 노드를 무제한으로 만들면
마디가 빽빽해질수록 오디오 스레드가 밀려 끊긴다. 상한에 닿으면 가장 오래된
보이스를 짧게 페이드아웃시키고 자리를 넘긴다.

M2 의 샘플러는 보이스 하나가 훨씬 무거우므로(디코딩된 버퍼 + 필터) 이 값을
다시 재야 한다. 지금 값은 오실레이터 기준이다.

### latencyHint: "interactive"

버퍼를 키우면 폰에서 끊김은 줄지만 화면을 두드리고 소리가 나기까지가 눈에 띄게
늦다. 노트를 찍으며 확인하는 앱이라 반응 속도를 택했다. 끊김이 문제가 되면
그때 기기별로 조정한다.

### 모바일 오디오 잠금 해제를 M1 에 넣은 이유

iOS 사파리와 안드로이드 크롬은 사용자가 화면을 만지기 전에 소리를 내주지
않는다. 나중에 붙이면 "재생을 눌렀는데 아무 일도 안 일어난다" 를 매번 겪는다.

세 가지를 함께 처리한다.
- `resume()` 은 반드시 사용자 제스처 핸들러 안에서 부른다
- iOS 는 그것만으로 부족한 경우가 있어 1 샘플짜리 무음 버퍼를 한 번 재생한다
- 백그라운드에 갔다 오면 다시 `suspended` 가 되므로 `visibilitychange` 에서
  한 번 더 깨운다

해제되기 전까지는 안내 화면을 덮어 둔다. 조용히 실패하는 것보다 낫다.

### 악기 API 를 noteOn/noteOff 짝이 아니라 `play(..., durationSec)` 로

피아노롤은 노트 길이를 **연주 전에 이미 알고 있다.** 미리 알고 예약하면
M3 의 오프라인 렌더에서 실시간 이벤트 루프 없이 같은 코드로 렌더할 수 있다.
실시간 건반 입력이 생기면 그때 noteOn/noteOff 를 추가한다.

### 노트 오른쪽 끝(길이 조절) 판정은 폭에 비례시킨다

처음에 고정 20px 로 잡았다가 브라우저 점검에서 걸렸다. 1/16 노트는 화면에서
18px 남짓인데 거기에 20px 짜리 '끝' 판정을 붙이면 **노트 전체가 길이 조절
구역이 되어 옮기기가 아예 안 된다.**

폭의 35% 로 하되 최소 8px, 최대 20px 로 묶었다. 짧은 노트에서도 옮길 자리가
남고, 긴 노트에서 끝이 손가락보다 좁아지지도 않는다.

기본 확대도 96px/박, 기본 스냅 1/8 로 올렸다. 기본값에서 노트가 48px 이라
손가락으로 잡을 만하다.

### 빈 곳 탭으로 노트를 찍을 때 '누른 시간' 을 보지 않는다

처음에는 400ms 안에 떼야 노트가 찍히게 했는데, 조금 오래 눌렀다 떼면 아무 일도
일어나지 않아 앱이 고장 난 것처럼 보인다. 손가락이 움직이지 않았으면 찍는다.
(길게 누르기는 **이미 있는 노트를 지우는** 동작에만 쓴다)

---

## M2

### SF2/SF3 재생은 `spessasynth_lib` 로 (v4.3.14, Apache-2.0)

후보 셋의 현재 문서를 확인하고 골랐다. 기준은 ① SF2 직접 로드 ② 오프라인
렌더 가능 ③ 모바일 사파리/크롬 동작.

| | SF2 직접 로드 | 오프라인 렌더 | 비고 |
| --- | --- | --- | --- |
| **spessasynth_lib** | SF2·SF3·SFOGG·DLS | `OfflineAudioContext` 지원 명시 | Apache-2.0, 의존성 없음 |
| js-synthesizer | SF2 (SF3 는 다른 빌드) | 프레임 직접 생성 가능 | 코어가 **LGPL v2.1** libfluidsynth WASM |
| smplr | `Soundfont2Sampler` 로 가능 | 실시간 재생 위주, 명시 없음 | 범용 샘플러에 가깝다 |

`js-synthesizer` 를 뺀 결정적인 이유는 라이선스다. 라이브러리 자체는
BSD-3-Clause 지만 실제 소리를 내는 libfluidsynth WASM 이 LGPL v2.1 이다.
개인용으로는 문제없지만 나중에 배포 형태를 바꿀 때 발목을 잡는다.
spessasynth 는 Apache-2.0 한 장으로 끝난다.

`smplr` 은 SF2 를 읽긴 하지만 오프라인 렌더가 명시돼 있지 않다. M3 의 WAV
내보내기가 이 프로젝트의 핵심 기능이라 ②번을 만족 못 하면 탈락이다.

### 확인한 것: 스케줄러를 갈아엎지 않아도 된다

가장 걱정한 부분은 **타이밍**이었다. 샘플러의 noteOn 이 "지금 당장" 만
된다면, M1 에서 만든 lookahead 스케줄러(절대 시각으로 예약)를 버리고
setTimeout 으로 때려야 해서 박자가 흔들린다.

문서에는 안 나와서 설치하고 타입 정의를 직접 읽었다
(`node_modules/spessasynth_core/dist/index.d.ts:2951`).

```ts
interface SynthMethodOptions {
  /** The audio context time when the event should execute, in seconds. */
  time: number;
}
noteOn(channel, midiNote, velocity, eventOptions?: SynthMethodOptions): void
```

**절대 시각을 받는다.** M1 스케줄러가 계산한 `when` 을 그대로 넘기면 된다.
`Instrument` 인터페이스도 그대로 두고 구현체만 갈아끼운다. 인터페이스를
미리 갈라 둔 게 여기서 값을 했다.

### 트랙 ↔ MIDI 채널 ↔ 개별 출력

```ts
connectIndividualOutputs(audioNodes: AudioNode[]): void  // 정확히 16개
```

MIDI 채널 16개의 출력을 **따로** 뽑을 수 있다. 그래서

    트랙 n  →  MIDI 채널 n  →  개별 출력 n  →  트랙 게인/팬 → 마스터

로 잇는다. 트랙마다 소리가 갈라져 있으니 M3 의 **트랙별 WAV(스템) 내보내기**
와 M5 의 볼륨·팬·뮤트·솔로가 추가 작업 없이 이 배선 위에 얹힌다.

대신 **트랙은 최대 16개**라는 제약이 따라온다. 스펙 요구는 최소 4트랙이라
문제없고, 넘어가면 신스를 하나 더 띄우면 된다.

### 악기 교체는 `programChange` 한 줄

```ts
programChange(channel, programNumber, { time })
```

노트 데이터는 손대지 않는다. 트랙의 `source.presetId` 를 바꾸고 채널에
프로그램 체인지를 보내면 끝이다. "노트는 악기 정보를 갖지 않는다" 는
M1 의 원칙이 여기서 그대로 값을 한다.

프리셋 목록은 `synth.presetList: MIDIPatchFull[]` 로 이름·프로그램·뱅크가
나온다. "sax" 검색은 이 목록을 이름으로 거르는 것이다.

---

## M3

### WAV 내보내기가 MIDI 변환을 거친다 (돌아가는 길이 아니다)

오프라인 렌더를 평소처럼 짰다가 **완전한 무음**을 받았다. OfflineAudioContext
에 신스를 만들고 noteOn 을 예약했는데 peak 이 0 이었다. 원인은 라이브러리
문서에 한 줄로 적혀 있다.

    Chromium seems to ignore worklet messages for OfflineAudioContext.

오프라인에서는 워크렛으로 보낸 메시지(noteOn·programChange)가 그냥 버려진다.
그래서 연주 내용을 **미리 통째로 넘기는** 전용 입구가 따로 있다.

    startOfflineRender({ midiSequence, soundBankList, loopCount })

이게 MIDI 를 받기 때문에 WAV 를 뽑으려면 먼저 MIDI 로 바꿔야 한다. 처음엔
돌아가는 길처럼 보였는데, 오히려 이쪽이 낫다. **내려받은 .mid 와 뽑아낸
.wav 가 같은 바이트에서 나온 결과**라 둘이 어긋날 수가 없다. 스펙의 왕복
시험(FLM → 여기 → FLM)이 정확히 이 성질을 요구한다.

### SMF 읽기·쓰기를 직접 짰다

라이브러리에도 MIDI 기능이 있지만 직접 썼다.

  · 스펙이 "포맷 1, 트랙당 1채널" 을 못박았다. 남의 구현에 맡기면 확인이 어렵다
  · 읽기와 쓰기가 대칭이라 **브라우저 없이** 왕복 테스트가 된다

두 번째가 컸다. `scripts/midi-roundtrip.mjs` 는 Node 에서 1초 만에 돌고
14가지를 본다. 브라우저를 띄워야만 확인할 수 있으면 자주 못 돌린다.
그러려고 MIDI 변환을 오디오 계층에서 떼어 냈다 (`model/preset.ts` 가
그래서 생겼다).

### MIDI 채널 9번을 비운다 — 트랙 상한 15개

MIDI 채널 10(0부터 세면 9)은 GM 규약상 드럼 자리다. 트랙 번호를 채널 번호로
그냥 쓰면 **열 번째 트랙만 갑자기 타악기 소리가 난다.** 내보낸 .mid 를 다른
DAW 에서 열어도 마찬가지다.

9번을 건너뛴다. 쓸 수 있는 트랙이 15개가 되지만 요구는 최소 4개다.
드럼 트랙은 M5 에서 "이 트랙은 드럼" 을 켜서 9번에 놓는 식으로 붙이면 된다.

### 사운드폰트를 버퍼가 아니라 "가져오는 함수" 로 넘긴다

`startOfflineRender` 는 넘긴 ArrayBuffer 를 워크렛으로 transfer 해서 원본을
detach 시킨다. 같은 버퍼로 두 번 렌더하면 두 번째가 죽는다.

    Failed to execute 'postMessage' on 'MessagePort':
    ArrayBuffer at index 0 is already detached.

트랙별 WAV 는 트랙 수만큼 렌더하니 정확히 이 상황이라, 첫 트랙만 나오고
나머지가 전부 실패했다. `renderProject` 가 버퍼 대신 **함수**를 받게 바꿔서
렌더할 때마다 새로 읽어 온다. 부르는 쪽이 이 함정을 알 필요가 없어진다.

원본 File 을 들고 있다가 그때그때 다시 읽는다. ArrayBuffer 를 복사해 두면
수백 MB 짜리 사운드폰트가 메모리에 두 벌 남는데, File 은 디스크를 가리키는
손잡이라 거의 공짜다.

### 16비트 WAV

스펙이 못박기도 했고 실제로도 그게 맞다. FL Studio Mobile 을 비롯한 모바일
DAW 와 웬만한 데는 다 그냥 열린다. 32비트 float 는 편집 단계에서나 의미가
있고 받는 쪽에서 못 여는 경우가 생긴다.

### 프로젝트 저장은 localStorage 가 아니라 파일

브라우저 데이터를 지우면 같이 날아가고, 폰에서 만든 걸 컴퓨터로 옮길 수도
없다. 사용자가 손에 쥘 수 있는 파일이어야 한다. 읽을 때는 남의 파일이라 치고
검사한다 — 사람이 열어 고칠 수 있는 형식이라 오타가 들어오기 쉽고, 조용히
넘기면 노트가 사라진 채로 진행된다.

---

## M4

### 없는 음은 가장 가까운 샘플을 재생 속도로 민다

국악기 음원을 스무 개쯤 받아 봐야 두 옥타브가 다 차지 않는다. 빈 건반을
눌렀을 때 아무 일도 안 일어나면 사용자는 고장으로 받아들인다. 가장 가까운
샘플을 `playbackRate` 로 밀어서 채운다 (반음 = 2^(1/12) 배).

속도를 바꾸면 음색도 같이 변한다. 그래서 **반음 12개까지만** 밀고 그보다 멀면
소리를 내지 않는다. 한 옥타브를 넘겨 당겨 쓰면 악기가 아니라 장난감 소리가
난다. 샘플 고르기는 음높이를 세기 층보다 10배 무겁게 친다 — 음정이 틀리면
세기가 맞아 봐야 소용이 없다.

### 릴리즈 꼬리 0.35초

뜯는 악기(가야금)는 손을 떼도 소리가 바로 안 끊긴다. 노트 길이에서 딱 잘라
버리면 "툭" 하고 끊겨서 악기로 안 들린다. 노트가 끝난 뒤에도 샘플을 조금 더
흘려보내며 지수 곡선으로 줄인다. 샘플에 여운이 이미 녹음돼 있으면 그게 그대로
들린다.

### 못 알아들은 파일을 버리지 않는다

파일명 규칙이 배포처마다 달라서 네 가지를 받는다 (음이름·MIDI 번호·올림표·
내림표, 세기 층 `_p`/`_mf`/`_f`). 그래도 못 알아듣는 이름이 나온다.

**조용히 빼면 안 된다.** 20개를 넣었는데 12개만 소리가 나면 왜 그런지 알 수가
없다. 못 알아들은 파일은 그대로 세워 두고, 파일을 넣은 직후에 건반을 고르는
화면을 띄운다. 나중에 찾아 들어가라고 하면 아무도 안 한다.

일련번호를 음높이로 착각하지 않는 것도 중요하다. 음이름을 먼저 찾고, 없을
때만 숫자를 MIDI 번호로 본다. `해금_2_A3.wav` 에서 "2" 를 잡으면 안 된다.

### 섞인 프로젝트는 나눠 렌더한 뒤 더한다

사운드폰트 트랙은 MIDI 를 통째로 넘겨야 하고(startOfflineRender), 폴더 트랙은
노트를 직접 꽂는다. 한 컨텍스트에 억지로 밀어 넣으면 startOfflineRender 가
컨텍스트를 가져가 버려 나머지가 묻힌다. 각각 렌더한 뒤 샘플을 더한다.

AudioBuffer 는 컨텍스트를 가리지 않는 순수한 데이터라, 오프라인 렌더에서
WAV 를 다시 디코딩하지 않고 이미 읽어 둔 것을 그대로 넘긴다 (`adopt`).

---

## M5

### 솔로는 저장하지 않는다

뮤트와 솔로는 성격이 다르다. 뮤트는 "이 트랙은 빼고 간다" 는 **곡에 대한
결정**이라 파일에 남긴다. 솔로는 "지금 이것만 들어 보자" 는 **작업 중의
상태**다. 저장했다 다음 날 열었는데 한 트랙만 들리면 곡이 망가진 줄 안다.

그래서 `Track` 에는 `muted` 만 있고 솔로는 `MixerState` 가 메모리로만 들고
있다. 들리는지 여부(`isAudible`)를 계산하는 곳도 거기 한 군데다 — 재생·
미리듣기·오프라인 렌더 세 군데에 같은 규칙을 흩어 놓으면 반드시 어긋난다.

### 믹서 하나가 재생과 렌더를 모두 먹인다

`connectIndividualOutputs` 가 OfflineAudioContext 에서도 살아 있는지 먼저
확인했다 (채널 하나만 켜고 렌더해서 소리가 나는지 봤다). 살아 있었다.

그래서 오프라인 렌더도 마스터로 한 번에 받지 않고 같은 `Mixer` 를 거친다.
**화면에서 들은 균형이 뽑아낸 WAV 와 같다.** 이게 아니면 "들을 때는 괜찮았는데
파일로 뽑으니 다르다" 가 되는데, 그건 고치기도 어렵고 믿을 수도 없다.

### 리버브 임펄스를 계산으로 만든다

보통은 실제 공간에서 녹음한 IR 파일을 쓰지만 그러면 음원 파일이 하나 더
생기고 라이선스가 따라온다. 이 프로그램은 사용자가 넣는 음원 말고는 아무
파일도 들고 있지 않기로 했다. 잡음을 지수로 감쇠시켜 방 울림을 흉내 낸다.

센드를 팬 **뒤**에 뒀다. 볼륨을 내리면 울림도 같이 줄어야 자연스럽다. 앞에
두면 소리를 완전히 줄여도 울림만 남아 유령처럼 들린다.

### 되돌리기는 스냅숏 방식

무엇이 어떻게 바뀌었는지 기록하는(커맨드 패턴) 쪽이 메모리는 덜 먹지만,
편집 종류가 늘 때마다 되돌리는 코드도 같이 늘고 **하나만 빠뜨려도 조용히
깨진다.** 프로젝트가 노트 수천 개라도 JSON 몇백 KB 라서 틀릴 일이 없는 쪽을
골랐다.

핵심은 **언제 찍느냐**다. 노트를 끄는 동안 프레임마다 찍으면 되돌리기 한 번에
1픽셀씩 돌아간다. 화면은 "이제 뭔가 바꾼다" 는 순간(손가락을 대는 순간)에만
알려 주고 그때의 직전 상태를 쌓는다. 그래서 드래그 한 번이 되돌리기 한 번이다
— 점검에서 이걸 직접 확인한다.

### 단축키는 최소한만

폰이 주 환경이다. 스페이스(재생·정지), Ctrl+Z / Ctrl+Shift+Z, Esc(창 닫기)
뿐이다. **화면에서 되는 일을 키보드로도 되게 하는 것**이지 키보드로만 되는
기능을 만들지 않는다 — 그러면 폰에서 못 쓴다. 스펙의 "데스크톱 전용
인터랙션에 기능을 숨기지 말 것" 이 이 얘기다.

---

## 폰에서 접속하려면 https 여야 한다

문서를 쓰다가 확인해 보고 알았다. **컴퓨터에서 서버를 띄우고 폰으로 붙는
방법에 구멍이 있었다.**

| 주소 | isSecureContext | 사운드폰트 |
| --- | --- | --- |
| `http://localhost:5173` | true | 됨 |
| `http://192.168.0.5:5173` | **false** | **안 됨** |

AudioWorklet 은 보안 컨텍스트에서만 존재한다. `localhost` 는 예외로 인정받지만
IP 로 붙으면 아니다. 폰에서 같은 와이파이의 컴퓨터에 접속하는 게 정확히 그
경우다. 그때까지 테스트를 전부 127.0.0.1 에서 해서 못 보고 있었다.

게다가 오류가 이렇게 났다.

    Cannot read properties of undefined (reading 'addModule')

사용자가 알아들을 수 없는 말이고 무엇을 하면 되는지도 없다. 이 저장소가
금지하는 종류의 실패다.

두 가지를 했다.
  · 접속하자마자 확인해서 **사람 말로** 알려 준다. 사운드폰트를 고르고 나서
    안 된다는 걸 알면 이미 시간을 쓴 뒤다
  · `npm run dev:https` 를 뒀다. 그 자리에서 만든 인증서라 브라우저가 경고하지만
    한 번 넘기면 보안 컨텍스트가 되어 사운드폰트가 열린다

localhost 가 아닌 호스트명 + 자체 서명 인증서로 실제로 확인했다 —
`isSecureContext: true`, 프리셋 7개 로드 성공.

**낱개 WAV 폴더와 임시 신스는 http 에서도 된다.** 워크렛을 안 쓰기 때문이다.
그래서 안내에도 그 사실을 같이 적는다 — 아무것도 안 된다고 오해하면 앱을
닫아 버린다.

---

## 드럼 킷이 일반 악기와 겹치던 것

GeneralUser GS(실제 GM 사운드폰트)를 넣어 보고 알았다. 목록이 이렇게 나왔다.

    0:0  Grand Piano
    0:0  Standard 1      ← 드럼 킷인데 번호가 똑같다

presetId 를 `뱅크 × 128 + 프로그램` 으로 담았는데, **드럼 킷은 일반 악기와
뱅크·프로그램이 그대로 겹친다.** 그래서 목록에서 드럼을 골라도 피아노가
걸렸다. 화면이 거짓말을 하는 셈이라 사용자는 무슨 일이 벌어졌는지 알 수가
없다.

라이브러리는 `MIDIPatchFull.isDrum` 으로 알려 준다 (문서가 이걸 권한다 —
`isGMGSDrum` 은 GM/GS 만 잡고 XG 드럼을 놓친다).

### 드럼 표시를 뱅크에 우겨넣지 않는다

처음엔 "드럼 = 128번 뱅크" 로 담았다. 진짜 뱅크는 127 을 넘지 않으니 겹칠 일이
없다고 봤는데, 다시 걸렸다. GeneralUser GS 는 드럼 킷이 26개인데 **그것들도
뱅크가 여럿**이라, 드럼의 뱅크를 뭉개니 이번엔 드럼끼리 13개가 겹쳤다.

드럼 여부와 뱅크는 서로 다른 정보다. 각자 자리를 준다.

    presetId = (드럼이면 16384) + 뱅크 × 128 + 프로그램

287개 프리셋 전부 고유해졌다.

### 드럼 트랙은 MIDI 9번 채널로 보낸다

신스에 "이 채널은 드럼" 이라고 따로 말하는 길(`setDrums`)도 있지만, 그러면
**재생만 맞고 내보낸 MIDI 는 안 맞는다.** 다른 DAW 는 그 지시를 못 듣는다.

9번 채널에 놓으면 GM 규약 하나로 재생·WAV 렌더·MIDI 내보내기가 한꺼번에
맞는다. 원래 9번을 비워 두던 자리(model/channels.ts)에 드럼이 들어가는 셈이라
구조도 그대로다. 채널 배치를 `assignChannels(project)` 한 곳으로 모았다 —
드럼은 9번, 나머지는 9번을 건너뛰고 순서대로.

---

## 누르면 소리가 늦게 나던 것

"약간 레이턴시가 있다" 는 말을 듣고 재 봤다. 지연을 셋으로 갈라서 봤다.

    오디오 파이프라인 (baseLatency 10ms + outputLatency 32ms)   42ms
    우리가 예약하는 오프셋                                        5ms
    격자를 누르고 소리가 날 때까지                              124.7ms  ←

앞의 둘은 작은데 마지막이 컸다. 이유는 단순했다. **소리를 손가락을 뗄 때
냈다.** 노트가 실제로 찍히는 시점(`addNoteAt`)에서 미리듣기를 불렀는데, 그건
탭이 끝나야 알 수 있으니 pointerup 이다. 손가락을 123.6ms 대고 있었더니 소리는
124.7ms 에 났다 — **누르고 있는 시간이 그대로 지연이 된다.** 천천히 누를수록
더 늦어진다.

손가락을 대는 순간(pointerdown)으로 옮겼다. 0.3ms 가 됐다.

화면을 밀려고 눌렀을 때도 소리가 한 번 나게 됐지만, 짧고 오히려 자연스럽다.
건반은 누르면 나야 악기로 느껴진다. 노트를 찍는 쪽에서는 미리듣기를 뺐다 —
안 그러면 한 번 누르는데 두 번 울린다.

남은 42ms 는 브라우저와 OS 의 오디오 경로라 앱이 어쩌지 못한다. 이미
`latencyHint: "interactive"` 로 최소를 요청하고 있다. 버퍼를 더 줄이면 폰에서
끊기기 시작하는데, 그건 늦는 것보다 나쁘다.

곁다리로 신스의 이벤트 시스템을 껐다(`eventsEnabled: false`). 워크렛이 노트마다
메인 스레드로 알림을 보내는데 우리는 듣는 쪽이 없어 그대로 버려진다.

조용히 되돌아가기 쉬운 종류라 점검에 못 박았다 — 150ms 눌렀다 뗐을 때 소리가
30ms 안에 나야 통과다.

---

## 재생과 렌더의 음량이 달랐다 (세제곱으로)

"WAV 로 뽑은 게 재생이랑 똑같이 들리는지 — 렌더 경로가 따로면 리버브나 볼륨이
미묘하게 달라지는 일이 진짜 많다" 는 지적을 받고 재 봤다. 실제로 그랬다.

볼륨을 바꿔 가며 렌더 피크를 재면 이렇게 나와야 한다.

    볼륨 0.5  → 0.5배
    볼륨 0.25 → 0.25배

실측은 **0.127배 · 0.016배** 였다. 세제곱이다 (0.5³ = 0.125, 0.25³ = 0.0156).

원인은 음량이 세 겹으로 걸린 것이다. 렌더는 프로젝트를 MIDI 로 바꿔
`startOfflineRender` 에 먹이는데(M3 참고), 그 MIDI 에 CC7(볼륨)을 실어 보내고
있었다. 신스가 그걸로 한 번 줄이고 — **GM 의 CC7 은 제곱 곡선이라 그것만으로
제곱이다** — 우리 믹서가 또 한 번 곱했다.

기본값인 볼륨 0.8 에서 WAV 가 재생의 약 절반 음량으로 나갔다.

MIDI 를 두 용도로 쓰는 게 원인이라 용도를 갈랐다.

  · **파일로 내보낼 때**는 CC7/CC10 을 싣는다. 다른 DAW 에서 열었을 때 균형이
    유지되어야 한다
  · **우리 렌더에 먹일 때**는 싣지 않는다. 믹서가 이미 걸고 있다

고친 뒤 실측 0.5배 · 0.25배. 점검에 못 박았다.

## 확인해 본 것들 (2026-08)

지적받은 항목을 실제로 돌려 본 결과다.

| 확인한 것 | 결과 |
| --- | --- |
| 16마디(64박) 끝까지 재생했을 때 밀림 | **0.000ms**. 과거로 예약된 노트 0회 |
| 동시 24음 (SF2) | 피크 0.42, 잘린 샘플 0 |
| 동시 32음 (임시 신스, 폴리포니 상한 24) | 피크 0.66, 잘린 샘플 0 |
| 101MB 사운드폰트 로드 | 2.0초, JS 힙 25→24MB |
| 셋잇단음 MIDI 왕복 | 정확히 보존 (PPQ 480 이 3으로 나누어떨어진다) |

타이밍은 `when` 을 절대 시각으로 예약하고 그 값을 그대로 기록해서 이상적인
격자와 비교했다. 스케줄러가 늦게 깨어나 **과거로 예약**하면 `Math.max(when, now)`
가 현재로 당겨 버려 리듬이 뭉개지는데, 그 횟수도 같이 셌다. 0이었다.

101MB 가 메모리를 안 먹는 이유는 ArrayBuffer 가 워크렛으로 transfer 되어 메인
스레드에 남지 않기 때문이다. 내보낼 때 원본 File 에서 다시 읽는 구조(M3 의
detach 버그를 고치며 잡은 것)가 여기서 값을 했다. 대신 **트랙별 WAV 는 트랙
수만큼 다시 읽는다** — 수백 MB 를 두 벌 들고 있지 않는 대가다.

## 셋잇단음 스냅

한 박을 셋으로 나눈 자리는 1/16 격자로는 **절대 못 찍는다.** 0.25 격자에
0.333… 을 올릴 방법이 없다. 1/8 셋잇단(1/3박)과 1/16 셋잇단(1/6박)을 넣었다.

소수점이 안 떨어지지만 MIDI 로는 정확하다. 4분음표를 480틱으로 쓰는데 480 은
3으로 나누어떨어져서 1/3박 = 160틱, 1/6박 = 80틱으로 정수가 된다. 다른 DAW 에서
열어도 어긋나지 않는다 — 왕복 테스트로 확인했다.

---

## 렌더 경로가 둘로 갈린 문제 — SF2 트랙이 497ms 당겨져 있었다

"SF2 트랙 하나 + 폴더 샘플러 트랙 하나를 같이 찍어놓고 WAV 를 뽑아본 적 있어?
각각 잘 되더라도 합칠 때 몇 ms 어긋나면 가야금만 살짝 밀려서 들려" 라는 지적을
받았다. 섞어서 뽑아 본 적은 있었지만 **피크만 비교했지 정렬은 안 봤다.**

재 봤더니 몇 ms 가 아니었다.

### 왜 갈려 있나

MIDI 로는 커스텀 샘플을 표현할 방법이 없다. 그래서

  · SF2 트랙   → MIDI 로 바꿔 `startOfflineRender` (M3 참고)
  · 폴더 트랙  → 별도 컨텍스트에 노트를 직접 꽂는다

각각 렌더한 뒤 샘플을 더한다.

### 재 본 결과

1박(120BPM 에서 0.5초 = 22050번째 샘플)에 같은 노트를 놓고 첫 유효 샘플을 찾았다.

    폴더 샘플러 → 22062번째   (이론값 22050, 정확)
    SF2        →   136번째   (맨 앞으로 당겨짐)

**497ms.** 원인은 시퀀서의 `skipToFirstNoteOn` 이 기본으로 켜져 있는 것이었다.
MIDI 플레이어에서는 맞는 편의 기능이다 — 파일을 틀었는데 앞의 3초 무음을
기다리게 하지 않으니까. 그런데 우리는 플레이어가 아니라 편집기다.

껐다. 497ms → 2.1ms.

트랙 하나만 뽑아 보면 "좀 일찍 시작하네" 정도라 그냥 넘어간다. 두 종류를 같이
뽑아야 드러난다. 지적이 정확했다.

### 남은 2.1ms 는 블록 양자화다

여러 위치에서 재 보니 고정값이 아니었다.

    120BPM 1박   95샘플 늦음
    120BPM 2박   61샘플
    120BPM 3.5박 10샘플
     90BPM 1박   41샘플
    160BPM 2박   78샘플

전부 **128샘플 미만**이다. 128샘플 = Web Audio 의 렌더 퀀텀(2.9ms). spessasynth
시퀀서가 MIDI 이벤트를 블록 경계에서 처리한다는 뜻이다. 폴더 샘플러는
`AudioBufferSourceNode.start(when)` 이라 샘플 단위로 정확하다.

라이브러리 바깥에서 고칠 수 있는 게 아니다. 최대 2.9ms 이고 플램이 들리기
시작하는 10ms 보다 한참 아래라 그대로 뒀다. 점검에는 "렌더 퀀텀 1개 이내" 로
못 박았다 — 다시 벌어지면 잡힌다.

### 한 경로로 몰 수도 있다 (아직 안 함)

폴더 샘플을 메모리에서 SF2 로 조립해 하나의 `startOfflineRender` 에 넣으면 두
경로가 같은 양자화를 겪으므로 **상대 오차가 0** 이 된다. 라이브러리에 SF2 쓰기
기능이 있어서(테스트 픽스처를 그렇게 만든다) 불가능하지 않다.

지금은 안 했다. 얻는 게 2.9ms 인데, 임의의 WAV 묶음을 키 범위·루트 키·세기
층까지 갖춘 사운드폰트로 조립하고 사용자 뱅크와 프리셋 번호가 겹치지 않게
합치는 일이 만만치 않다. 2.9ms 가 문제가 되는 순간 그때 하면 된다.

## 트랙별 WAV 의 진행률

트랙 수만큼 렌더하고 렌더마다 사운드폰트를 다시 읽는다. 100MB 짜리로 4트랙을
뽑으면 400MB 를 읽는 셈이다. 수백 MB 를 메모리에 두 벌 들고 있지 않으려는
대가인데, 그동안 화면이 조용하면 사용자는 앱이 죽은 줄 알고 나가 버린다.

트랙 번호만이 아니라 **지금 무엇을 하고 있는지**까지 적는다 — 음원 읽는 중 /
소리 만드는 중 / 저장 중. 그리고 안내를 적은 뒤 화면이 한 번 그려질 틈을
준다(`nextFrame`). 안 그러면 적어 놓고 바로 렌더에 들어가서 안 보인다.

---

## 뜯는 악기의 여운을 자를지 말지를 재서 정한다

가야금은 손을 떼도 줄이 계속 울린다. 뜯으면 알아서 1~2초 울리다 잦아든다.
그런데 처음엔 노트 길이 + 0.35초에서 무조건 잘랐다.

    1/16 노트(120BPM 에서 0.125초) + 1.2초짜리 샘플
      → 0.475초에서 끊고 여운 0.7초를 버린다

가야금다움이 통째로 사라지는 자리다.

그렇다고 항상 끝까지 울리게 두면 부는 악기가 이상해진다. 3초짜리 지속음이
1/16 노트에서도 3초를 다 울린다.

**둘을 가르는 건 샘플 자체다.** 뜯는 소리는 뒤로 갈수록 작아지고, 부는 소리는
유지된다. 파일을 읽을 때 앞뒤 RMS 를 비교해서 정한다 — 앞(5~15% 구간) 대비
뒤쪽(55~65% 지점)이 절반 아래(-6dB)로 떨어지면 잦아드는 소리로 본다.

확인한 결과:

    가야금(뜯는, 1.2초 샘플)  decaying=true   1/16 노트 → 1.2초까지 울림
    대금(부는, 3.0초 샘플)    decaying=false  1/16 노트 → 0.425초에 멎음

추측으로 숫자를 바꾸지 않고 샘플에서 재는 쪽을 골랐다. 국악기는 악기마다
성질이 달라서 하나로 정할 수가 없다.

여운을 길게 남기면 보이스가 금방 쌓이므로 폴더 샘플러에도 상한(32)을 두고
오래된 것부터 뺏는다. 원래는 상한이 없었다.

## 알아 두어야 할 것 — 유니즌 더블링

SF2 트랙과 폴더 샘플러 트랙에 **완전히 같은 음높이**를 겹쳐 놓으면 2.9ms 안쪽의
어긋남이 위상 간섭을 만들어 소리가 얇아질 수 있다. 게다가 그 오차가 매번
달라서(측정값 61·10·41·78·95샘플) 뽑을 때마다 미세하게 다르다.

일부러 유니즌으로 겹치지만 않으면 만날 일이 없다. 겹쳐야 한다면 한쪽을
옥타브로 옮기거나 같은 경로(둘 다 SF2, 또는 둘 다 폴더)로 맞추면 된다.

## 국악기 음원을 넣을 때 미리 알아 둘 것

아직 실제 국립국악원 음원으로 확인하지 못했다. 사용자가 알려 준 주의점을
남겨 둔다.

- **파일명에 연주법이 붙어 있을 수 있다.** 서양 악기처럼 `가야금_C4` 가 아니라
  정악/산조, 뜯기·튕기기 같은 게 섞인다. 파서가 못 알아들으면 "미정" 으로
  세워 두니(조용히 버리지 않는다) 그 파일명을 보고 규칙을 추가하면 된다.
- **가야금은 12평균율이 아니다.** 전통 음률로 조율돼 있어 파일명의 음이름과
  실제 음높이가 미묘하게 어긋날 수 있다. 피치 시프트로 채운 음이 살짝 붕 뜬
  느낌이면 **버그가 아니라 악기 자체 특성**이다.
- **여운이 길다.** 위의 판정으로 다루지만, 실제 음원에서 어떻게 들리는지는
  귀로 확인해야 한다.

---

## 확대할 때마다 소리가 나던 것

지연을 없애려고 미리듣기를 "손가락 대는 즉시" 로 옮겼더니, **확대하거나 화면을
밀 때마다 소리가 났다.** 핀치도 밀기도 손가락 하나가 닿는 것으로 시작하기
때문에, 닿자마자 소리를 내면 피할 방법이 없다.

방법이 둘뿐이었다.

  · 즉시 내고, 제스처가 판명되면 소리를 끊는다 → 짧은 "틱" 이 남는다
  · 아주 잠깐 기다렸다가 낸다 → 그만큼 지연이 생긴다

두 번째를 골랐다. 45ms 기다렸다가 내고, 그 사이에 손가락이 움직이거나 두 번째
손가락이 오면 취소한다. 짧게라도 나는 것보다 아예 안 나는 게 낫다는 판단이다.

45ms 는 원래 문제(124ms)의 3분의 1이고, 오디오 파이프라인 자체가 42ms 라 체감
차이는 그보다 작다. 왼쪽 건반은 이 지연이 없다 — 거기서는 확대도 밀기도 안 하니
기다릴 이유가 없다.

## 재생 위치를 끌 수가 없었다

빨간 줄(재생 헤드)을 옮기려면 눈금을 **정확히 탭**하는 수밖에 없었다. 눈금에서
끌면 무조건 루프 구간이 잡혔기 때문이다. 헤드 자체를 끄는 방법이 아예 없었다.

셋을 고쳤다.

  · 눈금 높이 26 → 36px. 손끝이 닿는 면적이 40px 언저리라 26px 는 조준해야 한다
  · 헤드에 **손잡이**를 그렸다. 끌 수 있다는 걸 알려면 잡을 게 보여야 한다
  · 손잡이 근처(26px)를 끌면 헤드가 따라오고, 떨어진 곳을 끌면 예전처럼 루프
    구간이 잡힌다

둘 다 같은 눈금에서 하지만 무엇을 잡았는지로 갈린다. 루프를 잡는 기능을 없애지
않으면서 헤드를 끌 수 있게 하는 방법이었다.

---

## 복사·붙여넣기 — 선택 UI 를 새로 만들지 않았다

"마디마다 같은 코드를 다시 찍는 게 번거롭다" 는 요청에서 나왔다.

보통 DAW 는 사각형으로 범위를 끌어 고르는데, 그건 폰에서 **새 조작 방식을 하나
더 배우는 일**이다. 이 앱에는 이미 무엇을 가리키는 것과 어디에를 가리키는 것이
있고, 둘 다 눈금에서 손가락으로 잡는 것이며 이미 화면에 보인다.

    루프 구간  → 무엇을 복사할지 (없으면 헤드가 있는 마디)
    재생 헤드  → 어디에 붙일지

새로 배울 게 없다. 마침 직전에 헤드를 끌기 쉽게 고쳐 둔 것이 여기서 값을 했다.

### 붙여넣으면 헤드가 앞으로 간다 — 이게 핵심이다

없으면 마디마다 헤드를 옮기고 붙여넣기를 반복해야 해서 손이 두 배로 간다.
붙여넣은 만큼 헤드를 밀어 두면 **복사 한 번 하고 붙여넣기를 연달아 눌러**
여덟 마디를 채울 수 있다. 원래 불편했던 일이 탭 한 번씩이 된다.

### 복사할 때도 헤드를 옮긴다

처음엔 복사만 하고 헤드를 그대로 뒀다. 그랬더니 복사 직후 붙여넣기를 누르는
순간 **원본 위에 똑같은 노트가 겹쳐 쌓였다.** 점검에서 잡혔다 — 노트 12개가
생겼는데 시작점은 3개뿐이었다.

복사한 구간 끝으로 헤드를 옮긴다. 같은 것을 이어 붙이려는 게 목적이니 다음
자리에서 시작하는 게 맞다.

### 걸쳐 있는 노트는 안 가져간다

구간에 **시작점이 들어 있는** 노트만 뜬다. 걸친 노트를 잘라 가져가면 붙여넣었을
때 앞이 잘린 이상한 노트가 생긴다. 마디를 복사하는 용도라 그 마디에서 시작한
것만 가져가는 게 맞다.

### 붙여넣은 노트에는 새 id 를 준다

같은 id 가 둘이면 지우거나 옮길 때 엉뚱한 게 잡힌다. 곡 끝을 넘겨 붙이면
마디 수도 늘린다 — 조용히 잘라내면 붙인 게 사라진다.

---

## 내보내기가 조용히 실패하던 곳 셋

"거의 완성 같은데 버그 점검" 요청을 받고 아직 안 찔러 본 구석을 훑었다. 셋이
나왔고 **전부 조용한 실패**였다 — 이 저장소가 제일 금지하는 종류다.

### 1. 무음 WAV 경고가 성공 메시지에 덮여 있었다

경고를 띄우는 코드는 있었다. 그런데 바로 다음 줄이 이랬다.

    this.warnIfSilent(buffer);                      // 경고를 띄운다
    download(...);
    this.cb.onStatus(`... 를 저장했습니다`);          // 즉시 덮어쓴다

트랙을 전부 음소거하고 뽑으면 속이 빈 WAV 가 나오는데 화면에는 "저장했습니다"
만 떴다. **경고를 쓴 사람도, 읽는 사람도 없는 상태로 코드만 남아 있었다.**
안내를 저장 메시지보다 나중에 내도록 순서를 바꿨다.

### 2. 곡 길이 뒤의 노트가 말없이 빠졌다

마디 수를 1로 줄이고 4마디째에 노트가 있으면, WAV 에는 안 들어가는데 화면에는
그대로 보인다. MIDI 로 내보내면 또 들어간다. 같은 프로젝트에서 형식마다 결과가
다른데 아무 말이 없었다.

빠진 개수와 몇 마디까지 노트가 있는지를 적어 준다. 마디 수를 자동으로 늘리지는
않는다 — 사용자가 정한 길이를 앱이 마음대로 바꾸는 건 다른 종류의 놀람이다.

### 3. 음소거한 트랙도 스템 파일을 만들었다

속이 완전히 빈 WAV 가 나왔다. 다른 앱에 얹으려고 뽑는 파일인데 아무 소리도
없으면 쓸모가 없고 왜 비었는지도 알 수 없다. 빼고 뽑되 **몇 개를 뺐는지**
같이 알려 준다.

### 확인만 하고 넘어간 것들

같은 훑기에서 멀쩡하다고 확인한 것들이다.

  · 솔로가 걸린 채로 트랙별 WAV → 솔로를 무시하고 전부 제대로 나온다
  · 루프 구간이 곡보다 길어도 재생이 안 터진다
  · 음높이 0 과 127 을 렌더해도 안 터진다
  · SF2 트랙에서 복사한 노트를 폴더 샘플러 트랙에 붙여넣으면 그 악기로 난다
    (노트가 악기를 안 들고 있다는 원칙이 실제로 지켜지고 있다)

---

## 사운드폰트만 박자가 밀리던 것 (70.9ms)

> "임시 신스는 싱크가 안 밀리는데 목록에서 악기 불러오면 걔네는 싱크가 밀려"

들은 대로였다. 재 보니 **사운드폰트 트랙만 70.9ms 늦게** 울렸다.

    임시 신스        제시간
    낱개 WAV 폴더    제시간
    사운드폰트       70.9ms 늦음      ← 27초 동안 한 샘플도 안 변함

120BPM 에서 한 박의 14% 다. 혼자 들으면 모르고 **다른 트랙과 같이 들으면**
바로 어긋나게 들리는, 딱 사용자가 말한 그 증상이다.

### 왜 내보내기 검사에 안 걸렸나

이미 "두 렌더 경로가 정렬돼 있는가" 를 못 박아 뒀는데도 못 잡았다. 그건
**내보내기** 경로(`startOfflineRender`, 워크렛 안 시퀀서)를 본 검사였고,
화면에서 듣는 재생은 `noteOn(..., { time })` 으로 예약하는 **다른 경로**다.
경로가 둘이면 검사도 둘이어야 한다는 걸 또 배웠다.

### 원인

spessasynth 의 워크렛은 예약 시각을 **자기 시계**로 판정한다. 프로세서가
만들어진 순간의 시각을 시작점으로 잡고, 그 뒤로는 렌더한 샘플 수만큼 더한다
(`spessasynth_core` 의 `processMessage` / `processSplit`).

    if (time > this.currentTime) 큐에 넣는다
    else                        지금 처리한다

문제는 **프로세서가 만들어진 시점과 그래프가 그 노드를 실제로 돌리기 시작한
시점 사이의 틈**이다. 그 틈이 그대로 영구 오차로 남는다. 우리가 주는 시각은
AudioContext 시계 기준인데 신스는 그만큼 뒤처진 시계로 판정하니, 노트가 늘
그 틈만큼 늦게 울린다.

흘러가는 오차가 아니라 **처음에 한 번 어긋난 뒤 고정**되는 종류다. 27초 동안
여덟 번을 재도 70.9ms 에서 한 샘플도 안 움직였다.

### 고친 방법 — 상수로 박지 않고 잰다

값이 기기·부하마다 다르다(같은 코드에서 52ms · 61ms · 71ms 이 나왔다).
그래서 사운드폰트를 읽고 나면 **실제로 재서** 그만큼 당겨 준다
(`audio/soundfont.ts` 의 `calibrate`).

재는 법이 좀 특이하다. **소리를 내지 않고 잰다.**

  · 채널 15번을 믹서에서 잠깐 떼어 **분석기에만** 물린다 → 스피커로 안 간다
  · 같은 시각에 기준 오실레이터를 예약한다 (이것도 분석기에만 연결)
  · 두 분석기를 **같은 틱에** 읽으면 버퍼 끝 시각이 같다.
    두 파형이 시작한 샘플 번호의 차이가 곧 어긋난 양이다

세 번 재고 **첫 회는 버린다.** 프로그램 체인지 뒤 첫 음은 워크렛이 샘플을
처음 만지느라 따로 늦다(같은 조건에서 123ms 대 55ms). 그건 매번 생기는
지연이 아니라서 보정값으로 삼으면 나머지가 전부 앞으로 당겨진다.
남은 두 값 중 **작은 쪽**을 쓴다 — 측정 오차는 늦는 쪽으로만 생긴다.
0.002초~0.4초 밖의 값은 안 믿는다. 잘못 잰 값으로 박자를 미는 건 안 고친
것보다 나쁘다.

고친 뒤 다시 재니 **±3ms** 안으로 들어왔다. 남는 오차는 이미 받아들이기로 한
렌더 퀀텀 2.9ms 와 같은 크기다.

### 곁다리로 같이 나아진 것

건반을 눌렀을 때 사운드폰트 소리가 나기까지도 같은 70ms 를 먹고 있었다.
미리듣기를 pointerdown 으로 옮겨 놨는데도 사운드폰트만 유독 굼떴던 게 이거다.
같이 사라졌다.

### 기다리게 하지 않는다

재는 데 2초쯤 걸린다. 그동안 악기 목록을 못 보고 있으면 "멈춘 줄 알았다" 가
되므로 **기다리지 않는다.** 목록은 즉시 주고 보정은 뒤에서 돌린 다음, 끝나면
상태 줄에 `· 박자 62ms 보정` 을 덧붙인다. 폰마다 다른 값이라 숫자를 사용자가
볼 수 있는 자리에 남겨 둔다.


---

## 맨 앞으로 (⏮) 와 "누른 만큼 소리 나기"

### ⏮ 은 루프가 켜져 있으면 0박이 아니라 루프 시작으로 간다

처음부터 다시 들으려면 빨간 줄을 0박에 정확히 맞혀야 했다. 손가락으로 그걸
맞히는 건 어렵다.

루프가 켜져 있을 때 0박으로 보내면 **빨간 줄이 거짓말을 한다.** 재생은 어차피
루프 시작에서 시작하기 때문이다(`scheduler.play` 가 구간 밖이면 끌어온다).
보이는 자리와 들리는 자리가 다르면 그게 더 헷갈린다. 그래서 루프가 켜져 있으면
루프 시작으로 간다.

화면도 같이 앞으로 민다. 12마디쯤 보고 있다가 눌렀는데 빨간 줄이 화면 밖으로
가 버리면 아무 일도 안 일어난 것처럼 보인다.

### 곁다리로 잡은 것 — 재생 버튼이 거짓말하고 있었다

⏮ 을 붙이고 나서 재생 중에 눌러 봤더니 소리는 계속 나는데 버튼만 "▶︎ 재생"
으로 돌아갔다. 재생 위치를 옮기면 안에서 `stop()` → `play()` 를 하는데, 그
`stop()` 이 `onStop` 을 불러서 버튼을 되돌려 놓고 있었다.

⏮ 때문에 생긴 게 아니라 **원래 있던 버그**다. 빨간 줄을 끌 때도, 루프를 켜고
끌 때도 똑같이 났다. 다만 그 두 경우는 재생 중에 잘 안 해서 안 보였고, ⏮ 은
재생 중에 누르는 게 당연한 버튼이라 바로 드러났다.

재시작 중에는 알리지 않도록 했다(`Scheduler.restarting`). 되돌아가기 쉬운
종류라 점검에 못 박아 뒀다 — 고치기 전 코드로 돌려 보니 실제로 실패한다.

### 건반은 누르고 있는 동안 울린다

예전에는 무조건 0.25초짜리 한 방이었다. 가야금이나 색소폰처럼 길게 끄는 음원은
0.25초로는 무슨 소리인지 알 수가 없다 — **음원을 고르려고 누르는 자리인데
정작 그 판단을 못 하는** 상태였다.

`Instrument` 에 `hold`/`release` 를 넣었다. `play(..., durationSec)` 로는 낼 수
없는 소리다 — 손가락을 언제 뗄지 미리 알 수 없기 때문이다. 대신 이 둘은
**미리듣기 전용**이고 오프라인 렌더는 절대 쓰지 않는다. "길이를 미리 알고
예약한다" 는 렌더 쪽 원칙은 그대로 남는다.

세 가지를 정해야 했다.

**1. 톡 치고 떼면?** 손가락을 대었다 떼는 데 5ms 밖에 안 걸리는 경우가 있다.
그대로 끊으면 '틱' 하고 만다. 최소 250ms 는 울리게 했다(`MIN_PREVIEW_MS`).
짧게 누르면 예전과 똑같고, 길게 누르면 누른 만큼 울린다.

**2. 뜯는 소리는 붙잡지 않는다.** 여기가 함정이었다. 가야금을 붙잡아 두면
손을 뗄 때 `release` 가 여운을 잘라 먹는다 — 앞에서 어렵게 살려 놓은 바로 그
여운이다. 실제로도 줄은 뜯은 만큼만 울리지 손가락을 대고 있어도 길어지지 않는다.
그래서 `looksDecaying` 으로 판정된 샘플은 한 번 내고 놔둔다. 붙잡는 건 부는
소리(대금·해금)뿐이다. 점검에 못 박아 뒀다.

**3. 손 떼는 신호를 놓치면?** 브라우저가 `pointercancel` 을 안 주거나 화면이
꺼지면 소리가 영원히 남고 끌 방법이 없다. 어느 악기든 10초에서 끊는다
(`MAX_HOLD`). 미리듣기로 10초를 넘길 일은 없다.

루프 지점이 없는 낱개 WAV 는 샘플이 다 떨어지면 거기서 끝난다. 억지로 이어
붙이면 뚝뚝 끊기는 소리가 나서, 짧은 음원은 짧게 울리는 쪽을 골랐다.

곁다리로 미리듣기가 **한 번에 하나**가 됐다. 새 건반을 누르면 앞엣것이 끝난다.
예전에는 0.25초짜리가 겹쳐 쌓였다. 건반 위에서 손가락을 미끄러뜨리면 음이
따라오는 것도 이 구조라서 자연스럽게 붙었다.


---

## 가이드용 예제 곡

빈 화면으로 시작하면 이 앱이 뭘 하는 물건인지 알기가 어렵다. 노트를 몇 개
찍어 봐도 **"악기를 바꾼다"** 가 무슨 뜻인지는 여전히 안 보인다. 세 트랙이
들어 있는 8마디 곡을 하나 넣어 두면 그게 한 번에 보인다.

곡을 잘 쓰는 게 목적이 아니라서, **무엇을 보여 주는가**를 기준으로 짰다.

### D 장조 5음계만 쓴다

레·미·파#·라·시. 이 다섯 음 사이의 최소 간격이 **온음**이라 반음 부딪힘이
아예 생길 수가 없다. 사용자가 노트를 마구 끌어다 옮겨도 곡이 안 망가진다는
뜻이고, **만져 보라고 내놓는 곡**에는 그게 맞다. 가야금·거문고가 쓰는 음계라
이 앱이 겨냥한 국악기 음원과도 어울린다.

반주 화음도 D · Bm · A 세 개만 돌리되 전부 5음계 안에서만 골랐다.

### 드럼 트랙을 넣지 않았다

사운드폰트를 아직 안 넣은 사람이 예제를 열면 세 트랙이 전부 임시 신스로 난다.
그래도 곡으로 들려야 첫인상이 산다. 드럼은 음원이 없으면 낮은 톱니파
웅웅거림이 될 뿐이고, 애초에 "같은 노트를 다른 악기로" 라는 요점과도 상관이
없다 — 드럼은 바꿔 볼 수 있는 트랙이 아니다.

### 트랙 이름에 안내를 적지 않는다

처음에는 두 번째 트랙 이름을 "반주 — 여기를 바꿔 보세요" 로 지었다. 그런데
사운드폰트를 넣는 순간 `resnapTracks` 가 이름을 그 악기 이름으로 덮어쓴다.
**사라질 자리에 적어 둔 안내는 거짓말이 된다.** 안내는 상태 줄로 옮기고
이름은 "멜로디 · 반주 · 베이스" 로 두었다.

### 부를 때마다 새로 만든다

같은 객체를 돌려주면 사용자가 고친 게 다음에 열 때도 남는다. 그러면 예제가
예제 노릇을 못 한다. `demoSong()` 은 매번 노트 id 를 새로 뽑는다.

### 덮어쓰기 전에 물어본다

예제를 여는 건 지금 만들던 것을 통째로 날리는 일이다. 노트가 하나라도 있으면
확인을 받는다. 빈 화면이면 그냥 연다 — 아무것도 없는데 물어보는 건 그냥
귀찮게 하는 것이다.

### 손으로 적은 음표는 검사로 받친다

멜로디는 음표 목록을 손으로 적었다. 고치다 한 줄 어긋나기 쉽고, 어긋나도
소리로는 잘 안 들린다. 그래서 헤더가 약속한 것을 그대로 검사에 옮겼다
(`scripts/audit.mjs`, 브라우저 없이 돈다).

  · 전부 1/8 격자에 떨어지는가 (기본 스냅이 1/8 이다)
  · 곡 길이 안에 들어가는가
  · 5음계 밖의 음이 없는가
  · **같은 트랙에서 같은 음이 겹치지 않는가** — 겹치면 앞 음의 noteOff 가
    뒤 음을 끊는다. "가끔 한 음이 짧다" 정도로만 들려서 눈으로는 못 잡는다
  · 세 트랙이 서로 다른 악기인가
  · MIDI 왕복을 견디는가

시험용 SF2 에는 프리셋이 일곱 개뿐이라 GM 번호(65·107·32)가 없다. 그때는
앱이 첫 악기로 떨어뜨리는 게 맞는 동작이라, 악기가 셋 다 다른지는 브라우저
없는 audit 쪽에서 본다.


---

## 떨림 (비브라토)

> "보카로 조교할 때 목소리의 떨림을 보정하듯이 ... 트럼펫 같은 악기는 불면서
> 기교를 부리기도 하니까"

트럼펫이나 해금은 긴 음을 그냥 뻗지 않는다. 소리를 내다가 음정을 잘게 흔든다.
그게 없으면 아무리 좋은 음원이어도 "찍은 것" 처럼 들린다.

### 빠르기 슬라이더는 만들지 않았다 — 만들 수가 없어서

만들기 전에 먼저 쟀다. 사운드폰트 재생은 우리가 음정을 직접 흔드는 게 아니라
**신스에게 시킨다.** 어느 신호가 실제로 먹는지 확인해야 했다.

라(440Hz)를 길게 누르고 자기상관으로 음정을 추적한 결과다.

    설정                     흔들림 폭      빠르기
    ------------------------------------------------
    아무것도 안 함             0 cent        —
    CC1 = 64                34 cent      8.3 Hz
    CC1 = 127               86 cent      8.1 Hz
    CC76(빠르기) 20 / 100     변화 없음     8.1 Hz
    CC77(깊이) 단독           변화 없음      —

**CC1(모듈레이션 휠)은 깊이를 정확히 따라온다.** 반면 빠르기를 바꾸는 CC76 은
아무 반응이 없다 — GS 시스템 익스클루시브로 켜야 하는 모듈레이터라서 그렇다.
8.1Hz 는 SF2 규격의 기본 비브라토 속도(8.176Hz)와 사실상 같은 값이다.

그래서 빠르기 슬라이더를 안 만들었다. 만들면 임시 신스와 낱개 WAV 에서는 먹고
사운드폰트에서만 조용히 무시된다. **아무 말도 없이 안 먹는 조절기**는 이
저장소가 제일 싫어하는 종류다.

어차피 흔드는 속도는 악기의 성질에 가깝다 — 트럼펫이 얼마나 빨리 떠는지는 그
트럼펫이 정할 일이다. 대신 임시 신스와 낱개 WAV 도 같은 8.1Hz 로 흔들게 맞춰서
트랙을 바꿔 가며 들어도 셋이 따로 놀지 않는다.

### "떨림 시작" 이 사실 절반이다

실제 연주자는 모든 음을 떨지 않는다. 긴 음만 뻗다가 흔든다. 딜레이가 없으면
16분음표까지 전부 떨어서 기계처럼 들린다. 늦게 시작하게 두면 짧은 음은 저절로
안 떨린다 — 음표마다 켜고 끌 필요가 없어진다.

그래서 깊이와 딜레이를 늘 짝으로 둔다. 깊이가 0 보다 커지면 믹서에 "떨림 시작"
줄이 따라 생긴다.

### 노트마다가 아니라 트랙마다

보카로 조교는 음표 하나하나에 붙이지만, 폰 화면에서 노트를 붙잡고 곡선을
그리는 건 현실적이지 않다. 트럼펫이 얼마나 떠는지는 그 트럼펫의 성질에 가깝고,
"짧은 음은 안 떨림" 은 위의 딜레이가 이미 해결한다.

### 화음에서 떨림이 끊기지 않게

CC1 은 **채널 전체**에 걸리는 값이다. 딜레이를 주려면 음이 시작할 때 0 으로
되돌렸다가 서서히 열어야 하는데, 그러면 아직 울리고 있는 앞 음의 떨림까지 같이
꺼진다. 화음이나 이어지는 음에서 떨림이 뚝뚝 끊긴다.

그래서 **앞 음이 아직 안 끝났으면 되돌리지 않는다**(`vibBusyUntil`). 재생과
MIDI 내보내기가 같은 규칙을 쓴다.

딜레이가 0 일 때는 아예 노트마다 보내지 않는다. 채널에 한 번 걸고 끝이다.

### 렌더용 MIDI 에서 CC1 을 빼면 안 된다

볼륨(CC7)·팬(CC10)은 렌더용 MIDI 에서 뺀다. 믹서가 이미 걸고 있어서 두 번
줄어들기 때문이다. **떨림은 반대다.** 사운드폰트 트랙의 떨림을 실어 나르는
길이 CC1 하나뿐이라, 같이 뺐으면 화면에서는 떨리는데 뽑아낸 WAV 만 안 떨렸을
것이다. 헷갈리기 딱 좋은 자리라 검사로 못 박아 뒀다.

### 검사를 어떻게 했나

음원 경로가 셋인데(사운드폰트 · 임시 신스 · 낱개 WAV) 하나만 안 먹으면
슬라이더가 거짓말을 한다. 그래서 셋 다 **실제로 렌더해서 음정을 추적**한다.

    떨림 없음   9센트 안쪽 (음정 추적기 자체의 오차)
    떨림 최대   90~120센트

열 배 차이라 회귀가 나면 바로 걸린다. 추적기는 자기상관인데 두 군데를 손봐야
했다.

  · **순수한 사인파는 주기의 배수마다 상관이 똑같이 높다.** 그냥 최대값을
    고르면 한 옥타브 아래를 짚는다. 최대값의 92% 를 넘는 **가장 짧은** 주기를
    고르게 했다
  · 정수 lag 만 쓰면 440Hz 근처에서 눈금이 17센트다 — 재려는 떨림보다 굵다.
    봉우리 좌우로 포물선을 맞춰 소수점 아래를 찾는다

이 과정에서 시험용 지속음 샘플의 버그도 하나 나왔다. 흔들림을 주파수에 곱하고
있어서(`f * (1 + 흔들림) * t`) 흔들리는 폭이 시간에 비례해 커졌다 — 3초 끝에서
반음이 넘게 벗어난다. 여운 판정 검사는 음량만 봐서 안 걸렸고, 음정을 재기
시작하니 바로 드러났다. 위상에 더하도록 고쳤다.


---

## 음 하나씩 조교하기 (꾸밈)

> "악기 하나에 떨림을 전부 부여한다는거지? 그러면 기교가 아니라 염소잖아.
> 꺾는 음같은걸 기대한건데"

맞는 지적이었다. 앞에서 만든 건 트랙에 떨림을 거는 것이라, 그 악기가 내는 긴
음이 **전부 똑같이** 떨었다. 그건 기교가 아니라 그 악기의 버릇이다. 실제
연주에서 기교는 음마다 다르게 들어간다 — 같은 음높이를 찍어도 어떤 건 밑에서
끌어올리고, 어떤 건 끝에서 흘려 내리고, 어떤 건 아무것도 안 한다.

그래서 꾸밈을 `Note` 로 내렸다.

### "노트는 악기 정보를 갖지 않는다" 를 어긴 게 아니다

이 저장소의 첫 번째 원칙이라 짚고 넘어간다. 꾸밈은 **악기가 아니라 연주
방법**이다. 색소폰으로 찍어 둔 꺾는 음을 가야금으로 바꾸면 가야금이 그 자리를
꺾어 준다 — 오히려 원칙이 지켜지는 쪽이다. 노트가 들고 있으면 안 되는 건
"이 음은 색소폰으로" 지 "이 음은 꺾어서" 가 아니다.

### 넷만 넣었다

    떨림      뒷부분에서 음정이 흔들린다
    끌어올림   아래에서 붙여 올려 시작한다 (추성 · 스쿱)
    흘러내림   끝에서 음정이 떨어진다 (퇴성 · 폴)
    꺾기      한 번 위로 꺾었다 제자리로 온다 (전성)

보카로의 자유 곡선은 안 넣었다. 폰에서 손가락으로 곡선을 그리는 건 정확도가
안 나오고, 위 넷이면 "이 음만 다르게" 는 이미 된다.

### 트랙 기본값과 음의 꾸밈이 만나는 규칙

한 줄로 잘랐다.

> **트랙의 기본 떨림은 꾸밈을 정하지 않은 음에만 걸린다.
> 꾸밈을 정하면(「그냥」 포함) 그 음은 오로지 그 꾸밈대로만 연주된다.**

"꺾으면서 동시에 떨기" 는 안 된다. 되게 하려면 음마다 두 개를 고르게 해야
하는데, 폰에서 음 하나를 눌러 고르는 화면에 선택지를 두 겹으로 쌓으면 아무도
안 쓴다. 트랙 기본값을 남겨 둔 이유도 같다 — 관악기처럼 긴 음이면 으레 떠는
악기를 음마다 찍는 건 노동이다. 기본으로 깔고 특별한 음만 눌러 바꾸는 게 실제
조교 방식이다.

### 왜 탭인가

노트 위에서 **탭은 원래 아무 일도 안 하던 동작**이다 (끌면 이동, 오른쪽 끝을
끌면 길이, 길게 누르면 삭제). 비어 있던 자리라 새 모드나 토글 버튼을 만들지
않고 그대로 썼다. 폰 화면에 버튼을 하나라도 덜 얹는 쪽이 낫다.

고르면 **즉시 들려준다.** 꾸밈은 이름만 보고는 무슨 소리인지 모른다. 귀로
고르는 것이지 목록에서 고르는 게 아니다.

노트 오른쪽 끝에 `〜 ↗ ↘ ∧` 를 찍는다. 안 보이면 "어디에 걸었더라" 를 하나씩
눌러 확인해야 하고, 그건 조교가 아니라 수색이다.

### 음정을 흔드는 두 갈래

**떨림만 신스에게 시킨다**(CC1). 8Hz 로 흔드는 걸 우리가 점으로 그리면 1초짜리
음 하나에 백 개가 넘는 이벤트가 나간다 — 폰에서 못 쓸 짓이다.

**나머지 셋은 음정 곡선**이라 점 몇 개면 된다. 임시 신스와 낱개 WAV 는
보이스마다 `detune` 을 직접 그리고, 사운드폰트는 피치 벤드로 그린다.

여기서 갈리는 게 하나 있다. **피치 벤드는 채널 전체에 걸린다.** 사운드폰트
트랙에서 겹친 음 중 하나만 꺾는 건 불가능하다 — 같이 휜다. 기교를 넣는 자리는
대개 멜로디 한 줄이라 실제로 걸릴 일은 드물지만, 안 되는 건 안 된다고 적어
둔다. 임시 신스와 낱개 WAV 는 보이스마다 따로 휘어서 이 제한이 없다.

휘는 폭은 ±200센트(온음)를 넘지 않는다. MIDI 피치 벤드의 기본 범위가 ±2반음
이라 넘기면 신스마다 다르게 들린다.

**다 그리면 반드시 가운데로 되돌린다.** 안 그러면 그 뒤의 음이 전부 휜 채로
난다 — 조용히 음정이 틀어지는, 제일 찾기 어려운 종류의 버그다. 재생과 MIDI
내보내기 양쪽에 같은 처리를 넣었다.

### 짧은 음에서도 모양이 살아야 한다

구간 길이를 초로 못 박지 않고 **음 길이의 비율과 초 중 짧은 쪽**을 쓴다.
0.1초짜리 16분음표에 0.13초짜리 끌어올림을 붙이면 음이 끝날 때까지 제 음정에
도착하지 못한다. 검사로 못 박아 뒀다.

### 검사

곡선은 손으로 그린 것이라 **부호 하나만 뒤집혀도 코드는 멀쩡히 돈다.**
끌어올림이 위에서 내려오고 있어도 "뭔가 휘긴 하네" 로 들려서 귀로도 잘 안
잡힌다. 그래서 두 층으로 본다.

  · 곡선 자체 (브라우저 없이) — 끌어올림은 아래에서 시작해 0 으로, 흘러내림은
    0 에서 시작해 아래로, 꺾기는 올라갔다 제자리로. 범위·짧은 음도 같이
  · 실제로 렌더한 소리 — 음정을 추적해서 시작 · 중간 · 끝의 센트를 잰다.
    끌어올림은 시작이 -100센트 아래, 중간은 제자리. 흘러내림은 그 반대

트랙 기본값과 꾸밈이 합쳐지는 규칙도 네 갈래 전부 검사에 넣었다. 여기가
어긋나면 "이 음만 밋밋하게" 가 안 되거나, 반대로 전부 떨어 버린다.

### 그런데 폰에서는 안 열렸다 — 합성 탭으로만 시험한 대가

> "안뜨는데"

검사는 전부 통과하는데 실제 폰에서는 창이 안 떴다. 두 군데가 새고 있었다.

**1. 노트 오른쪽 끝은 길이 조절 자리다.** 손가락이 굵어서 노트 오른쪽 20px 을
길이 조절로 잡아 두는데(`EDGE_GRAB`), 좁은 노트에서는 그게 폭의 3분의 1이다.
거기를 누르면 제스처가 `resize` 로 잡히고, `onUp` 에 그 갈래가 없어서 **아무
일도 안 일어났다.** 길이를 안 바꿨으면(움직이지 않았으면) 그것도 탭이다.

**2. 400ms 를 넘기면 탭이 아니었다.** `TAP_MS` 가 400ms 인데 길게 누르기는
480ms 다. 그 사이에 뗀 손가락은 조용히 무시됐다. 폰에서 작은 노트를 정확히
누르려면 그 정도는 걸린다. 480ms 를 넘겼으면 길게 누르기가 이미 지웠을
테니, 탭 쪽에는 시간 제한을 아예 걸지 않았다.

둘 다 **합성 탭으로만 시험해서** 놓쳤다. `touchscreen.tap()` 은 0ms 만에,
지정한 좌표에 정확히, 흔들림 없이 누른다. 사람 손가락은 300ms 쯤 걸리고 몇 px
흔들리고 노트 정중앙을 못 맞힌다. 검사를 고쳤다 — 이제 **누르고 300ms 기다린
뒤 3px 흔들어서 떼고**, 노트 오른쪽 끝도 눌러 본다. 고치기 전 코드로 되돌리면
실제로 실패한다.

교훈은 이 저장소가 이미 적어 둔 것과 같다. **함수가 불렸는지가 아니라 결과가
나오는지를 봐야 하고**, 터치 앱이면 그 "결과" 에는 사람 손가락의 부정확함까지
들어가야 한다.
